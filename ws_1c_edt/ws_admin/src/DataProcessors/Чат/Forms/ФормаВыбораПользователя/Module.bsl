
&НаКлиенте
Процедура ОК(Команда)
#Если НЕ ВебКлиент Тогда
	Результат = ПолучитьСтруктуруВозврата();
	Закрыть(Результат);
#КонецЕсли
КонецПроцедуры

&НаСервере
Функция ПолучитьСтруктуруВозврата()
	Возврат Новый Структура("UUID, ИмяПользователя, Пароль", XMLСтрока(Пользователь), Пользователь.Наименование, Пароль);
КонецФункции